@page "/profile"
@page "/profile/{id}"

<AuthorizeView>
    <Authorized>
        <SpinLoader IsLoading="@(loading)" Spinner="SpinnerType.Circle">
            <ContentTemplate>
                <ProfileComponent User=User />
            </ContentTemplate>
        </SpinLoader>
    </Authorized>
    <NotAuthorized>
        You have to login
    </NotAuthorized>
</AuthorizeView>